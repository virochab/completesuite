[project]
name = "llm-compliance"
version = "0.1.0"
description = "LLM compliance testing suite"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

